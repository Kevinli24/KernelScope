services:
  worker:
    environment:
      GPU_MODE: real
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
